<input class="form-control" data-format="{{ format }}" data-pattern="{{ pattern }}"{% if style.disabled %} disabled{% endif %} name="{{ style.name }}{% if grid %}@{{ data.id }}{% endif %}" placeholder="{{ style.placeholder }}" type="{{ type }}" value="{{ data[style.name] }}">
